<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
    <title>Dashboard</title>
</head>

<body>
    <?php require_once 'layout.phtml' ?>
    <?php renderHeader("Dashboard") ?>
    <?php $_SESSION['flash'] = null ?>

    <div class="dashboard-container">
        <!-- Next Exam Section -->
        <?php if (isset($next_exam) && $next_exam): ?>
        <div class="mb-5">
            <h2 class="h4 fw-bold mb-3">
                <i class="fas fa-clock text-primary me-2"></i>
                Your Next Exam
            </h2>
            <div class="card border-0 shadow-sm next-exam-card position-relative overflow-hidden">
                <div class="card-body p-4">
                    <!-- Countdown Badge -->
                    <div class="position-absolute top-0 end-0 m-3">
                        <span class="badge bg-danger rounded-pill px-3 py-2">
                            <i class="fas fa-hourglass-half me-1"></i>
                            <?= isset($next_exam['days_until']) ? $next_exam['days_until'] . ' days' : 'Coming Soon' ?>
                        </span>
                    </div>

                    <div class="row align-items-center">
                        <div class="col-md-8">
                            <h3 class="h5 fw-bold mb-2"><?= htmlspecialchars($next_exam['title'] ?? 'Exam Title') ?>
                            </h3>
                            <div class="d-flex flex-wrap gap-3 text-muted small">
                                <span>
                                    <i class="fas fa-book me-1"></i>
                                    <?= htmlspecialchars($next_exam['course_name'] ?? 'Course') ?>
                                </span>
                                <span>
                                    <i class="fas fa-calendar-alt me-1"></i>
                                    <?= isset($next_exam['start_date']) ? date('M d, Y', strtotime($next_exam['start_date'])) : 'Date TBD' ?>
                                </span>
                                <span>
                                    <i class="fas fa-stopwatch me-1"></i>
                                    <?= isset($next_exam['duration']) ? $next_exam['duration'] . ' min' : 'Duration TBD' ?>
                                </span>
                            </div>
                        </div>
                        <div class="col-md-4 text-md-end mt-3 mt-md-0">
                            <a href="/api/exams/<?= $next_exam['id'] ?? 0 ?>" class="btn btn-primary">
                                <i class="fas fa-arrow-right me-1"></i> Start Exam
                            </a>
                        </div>
                    </div>
                </div>
                <!-- Decorative gradient bar -->
                <div class="position-absolute bottom-0 start-0 w-100"
                    style="height: 4px; background: linear-gradient(90deg, var(--bs-primary), var(--gold));"></div>
            </div>
        </div>
        <?php else: ?>
        <div class="mb-5">
            <h2 class="h4 fw-bold mb-3">
                <i class="fas fa-clock text-primary me-2"></i>
                Your Next Exam
            </h2>
            <div class="card border-0 shadow-sm">
                <div class="card-body text-center py-5">
                    <i class="fas fa-check-circle text-success mb-3" style="font-size: 48px;"></i>
                    <h5 class="fw-bold">No Upcoming Exams</h5>
                    <p class="text-muted small">You're all caught up! Check back later for new exams.</p>
                </div>
            </div>
        </div>
        <?php endif; ?>

        <!-- Courses Section -->
        <div>
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h2 class="h4 fw-bold mb-0">
                    <i class="fas fa-graduation-cap text-primary me-2"></i>
                    Your Courses
                </h2>
                <span class="badge bg-primary rounded-pill px-3 py-2">
                    <?= isset($courses) ? count($courses) : 0 ?> Courses
                </span>
            </div>

            <?php if (isset($courses) && !empty($courses)): ?>
            <div class="courses-grid">
                <?php foreach ($courses as $course): ?>
                <a href="/api/courses/<?= $course['id'] ?? 0 ?>" class="course-card-link text-decoration-none">
                    <div class="card h-100 border-0 shadow-sm course-card">
                        <div class="card-body p-4">
                            <!-- Course Icon -->
                            <div class="course-icon mb-3">
                                <?php
                                        $icons = ['fa-book', 'fa-calculator', 'fa-flask', 'fa-globe', 'fa-music', 'fa-paint-brush', 'fa-code', 'fa-language'];
                                        $icon = $icons[($course['id'] ?? 0) % count($icons)];
                                        ?>
                                <i class="fas <?= $icon ?> text-primary"></i>
                            </div>

                            <!-- Course Title -->
                            <h5 class="card-title fw-bold mb-2">
                                <?= htmlspecialchars($course['name'] ?? 'Course Name') ?></h5>

                            <!-- Course Stats -->
                            <div class="d-flex flex-wrap gap-2 mb-3">
                                <?php if (isset($course['exam_count'])): ?>
                                <span class="badge bg-light text-dark">
                                    <i class="fas fa-file-alt me-1"></i>
                                    <?= $course['exam_count'] ?> Exams
                                </span>
                                <?php endif; ?>
                                <?php if (isset($course['progress'])): ?>
                                <span class="badge bg-light text-dark">
                                    <i class="fas fa-chart-line me-1"></i>
                                    <?= $course['progress'] ?>% Complete
                                </span>
                                <?php endif; ?>
                            </div>

                            <!-- Progress Bar -->
                            <?php if (isset($course['progress'])): ?>
                            <div class="progress" style="height: 6px;">
                                <div class="progress-bar" role="progressbar"
                                    style="width: <?= $course['progress'] ?>%; background: linear-gradient(90deg, var(--bs-primary), var(--gold));">
                                </div>
                            </div>
                            <?php endif; ?>

                            <!-- Arrow indicator -->
                            <div class="card-arrow mt-3">
                                <i class="fas fa-chevron-right text-muted"></i>
                            </div>
                        </div>
                    </div>
                </a>
                <?php endforeach; ?>
            </div>
            <?php else: ?>
            <div class="card border-0 shadow-sm">
                <div class="card-body text-center py-5">
                    <i class="fas fa-book-open text-muted mb-3" style="font-size: 48px;"></i>
                    <h5 class="fw-bold">No Courses Enrolled</h5>
                    <p class="text-muted small">You haven't enrolled in any courses yet.</p>
                    <a href="/api/courses" class="btn btn-primary mt-2">Browse Courses</a>
                </div>
            </div>
            <?php endif; ?>
        </div>
    </div>

    <style>
    /* Dashboard Container */
    .dashboard-container {
        max-width: 1200px;
        margin: 0 auto;
    }

    /* Next Exam Card */
    .next-exam-card {
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(247, 248, 252, 0.95) 100%);
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .next-exam-card:hover {
        transform: translateY(-4px);
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1) !important;
    }

    /* Courses Grid - Responsive */
    .courses-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
        gap: 20px;
    }

    /* Course Card Link */
    .course-card-link {
        display: block;
        text-decoration: none;
        color: inherit;
    }

    /* Course Card */
    .course-card {
        transition: all 0.3s ease;
        cursor: pointer;
        position: relative;
        overflow: hidden;
    }

    .course-card:hover {
        transform: translateY(-8px);
        box-shadow: 0 12px 40px rgba(0, 0, 0, 0.12) !important;
    }

    .course-card:hover .card-arrow i {
        transform: translateX(4px);
        color: var(--bs-primary) !important;
    }

    .course-card:hover .course-icon i {
        transform: scale(1.1);
        color: var(--bs-primary) !important;
    }

    /* Course Icon */
    .course-icon i {
        font-size: 28px;
        transition: all 0.3s ease;
        color: var(--bs-primary);
    }

    /* Card Arrow */
    .card-arrow i {
        transition: all 0.3s ease;
        font-size: 14px;
    }

    /* Card Click Effect */
    .course-card:active {
        transform: scale(0.98);
    }

    /* Responsive Breakpoints */

    /* Laptop (base) - 5 cards */
    @media (min-width: 1200px) {
        .courses-grid {
            grid-template-columns: repeat(5, 1fr);
            gap: 24px;
        }
    }

    /* Tablet */
    @media (min-width: 768px) and (max-width: 1199px) {
        .courses-grid {
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
        }
    }

    /* Mobile */
    @media (max-width: 767px) {
        .courses-grid {
            grid-template-columns: repeat(2, 1fr);
            gap: 16px;
        }

        .course-card .card-body {
            padding: 16px !important;
        }

        .course-icon i {
            font-size: 22px;
        }

        .next-exam-card .card-body {
            padding: 20px !important;
        }
    }

    /* Small Mobile */
    @media (max-width: 480px) {
        .courses-grid {
            grid-template-columns: 1fr;
            gap: 12px;
        }
    }

    /* Hover Effect for Next Exam Button */
    .next-exam-card .btn-primary {
        position: relative;
        overflow: hidden;
    }

    .next-exam-card .btn-primary::after {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 0;
        height: 0;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.2);
        transition: all 0.5s ease;
        transform: translate(-50%, -50%);
    }

    .next-exam-card .btn-primary:active::after {
        width: 300px;
        height: 300px;
    }
    </style>
    <?php renderFooter(); ?>
</body>

</html>