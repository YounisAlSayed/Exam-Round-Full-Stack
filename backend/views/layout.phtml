<?php

if (!function_exists('renderHeader')) {
    function renderHeader(?string $pageTitle = 'ITC'): void
    {
        $currentUser = $_SESSION['user'] ?? null;
        $flash = $_SESSION['flash'] ?? null;  ?>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= htmlspecialchars($pageTitle) ?></title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,500;9..144,600;9..144,700&family=Inter:wght@400;500;600;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="<?= BASE_PATH ?>/views/layout.css">
</head>
<style>
@keyframes slideDown {
    from {
        transform: translateY(-20px);
        opacity: 0;
    }

    to {
        transform: translateY(0);
        opacity: 1;
    }
}
</style>

<body>

    <nav class="navbar navbar-expand-md navbar-academic mb-4">
        <div class="container">
            <a class="navbar-brand" href="<?= BASE_PATH ?>/api/dashboard">ITC</a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav"
                aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="mainNav">
                <ul class="navbar-nav ms-auto mb-2 mb-md-0">
                    <?php if ($currentUser): ?>
                    <li class="nav-item">
                        <a class="nav-link" href="<?= BASE_PATH ?>/api/dashboard">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="<?= BASE_PATH ?>/api/profile">Profile</a>
                    </li>
                    <?php if ($currentUser['role'] === 'teacher'): ?>
                    <li class="nav-item">
                        <a class="nav-link" href="<?= BASE_PATH ?>/api/users/list">Users</a>
                    </li>
                    <?php endif; ?>
                    <li class="nav-item">
                        <a class="nav-link" href="<?= BASE_PATH ?>/api/logout">Log out</a>
                    </li>
                    <?php else: ?>
                    <li class="nav-item">
                        <a class="nav-link" href="<?= BASE_PATH ?>/api/login">Log in</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="<?= BASE_PATH ?>/api/signup">Sign up</a>
                    </li>
                    <?php endif; ?>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container pb-5">

        <!-- In your renderHeader() function -->
        <?php if ($flash): ?>
        <div class="position-fixed top-0 end-0 p-3" style="z-index: 9999;">
            <div class="toast align-items-center text-bg-success border-0 flash-toast" role="alert"
                aria-live="assertive" aria-atomic="true" data-bs-delay="5000" id="flashToast">
                <div class="toast-header d-flex justify-content-between">
                    <p>Success</p>
                    <p>Just Now</p>
                </div>
                <div class="d-flex">
                    <div class="toast-body">
                        <i class="fas fa-check-circle me-2"></i>
                        <?= htmlspecialchars($flash) ?>
                    </div>
                    <button type="button" class="btn-close btn-close-white me-2 m-auto"
                        data-bs-dismiss="toast"></button>
                </div>
            </div>
        </div>
        <?php endif; ?>

        <?php }
    }

    if (!function_exists('renderFooter')) {

        function renderFooter(): void
        { ?>

    </main>

    <footer class="border-top mt-5 py-4" style="border-color: var(--line) !important;">
        <div class="container text-center text-muted small">
            &copy; <?= date('Y') ?> ITC &mdash; Course Quiz Platform
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="<?= BASE_PATH ?>/views/layout.js"></script>
</body>

</html>

<?php }
    }