<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
    <title>Profile</title>
</head>

<body>
    <?php require_once __DIR__ . '/../layout.phtml'; ?>
    <?php renderHeader('Profile') ?>
    <?php
    // Clear flash messages on page load to prevent showing again on refresh
    if (isset($flash)) {
        unset($_SESSION['flash']);
        unset($_SESSION['error']);
        unset($_SESSION['success']);
    }
    ?>

    <div class="profile-container">
        <div class="row g-4">
            <!-- Profile Sidebar -->
            <div class="col-lg-4">
                <div class="card border-0 shadow-sm">
                    <div class="card-body text-center p-4">
                        <!-- Profile Image -->
                        <div class="profile-avatar mb-3">
                            <?php if (isset($user['image']) && $user['image']): ?>
                            <img src="<?= htmlspecialchars($user['image']) ?>"
                                alt="<?= htmlspecialchars($user['first_name'] ?? 'User') ?>" class="rounded-circle"
                                style="width: 120px; height: 120px; object-fit: cover; border: 4px solid var(--bs-primary);">
                            <?php else: ?>
                            <div class="avatar-placeholder rounded-circle d-flex align-items-center justify-content-center mx-auto"
                                style="width: 120px; height: 120px; background: lightgrey; color: white; font-size: 48px;">
                                <?= strtoupper(substr($user['first_name'] ?? 'U', 0, 1) . substr($user['last_name'] ?? 'r', 0, 1)) ?>
                            </div>
                            <?php endif; ?>
                        </div>

                        <!-- User Name -->
                        <h4 class="fw-bold mb-1">
                            <?= htmlspecialchars(($user['first_name'] ?? '') . ' ' . ($user['last_name'] ?? '')) ?>
                        </h4>

                        <!-- Role Badge -->
                        <span
                            class="badge <?= ($user['role'] ?? 'student') === 'teacher' ? 'bg-primary' : 'bg-success' ?> px-3 py-2 mb-3">
                            <i
                                class="fas <?= ($user['role'] ?? 'student') === 'teacher' ? 'fa-chalkboard-teacher' : 'fa-user-graduate' ?> me-1"></i>
                            <?= ucfirst($user['role'] ?? 'Student') ?>
                        </span>

                        <!-- Email -->
                        <p class="text-muted small mb-0">
                            <i class="fas fa-envelope me-2"></i>
                            <?= htmlspecialchars($user['email'] ?? 'No email') ?>
                        </p>

                        <!-- Stats -->
                        <hr class="my-3">
                        <div class="row g-0">
                            <div class="col-6">
                                <div class="stat-item">
                                    <h5 class="fw-bold mb-0"><?= $stats['total_exams'] ?? 0 ?></h5>
                                    <span class="text-muted small">Exams Taken</span>
                                </div>
                            </div>
                            <div class="col-6">
                                <div class="stat-item">
                                    <h5 class="fw-bold mb-0"><?= $stats['avg_score'] ?? 0 ?>%</h5>
                                    <span class="text-muted small">Avg Score</span>
                                </div>
                            </div>
                        </div>

                        <!-- Edit Profile Button -->
                        <button class="btn btn-outline-primary w-100 mt-3" data-bs-toggle="modal"
                            data-bs-target="#editProfileModal">
                            <i class="fas fa-edit me-2"></i>Edit Profile
                        </button>
                    </div>
                </div>
            </div>

            <!-- Profile Content -->
            <div class="col-lg-8">
                <!-- Tabs -->
                <ul class="nav nav-tabs mb-4" id="profileTabs" role="tablist">
                    <li class="nav-item" role="presentation">
                        <button class="nav-link active" id="overview-tab" data-bs-toggle="tab"
                            data-bs-target="#overview" type="button" role="tab">
                            <i class="fas fa-chart-simple me-2"></i>Overview
                        </button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="exams-tab" data-bs-toggle="tab" data-bs-target="#exams"
                            type="button" role="tab">
                            <i class="fas fa-file-alt me-2"></i>Exam History
                        </button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="courses-tab" data-bs-toggle="tab" data-bs-target="#courses"
                            type="button" role="tab">
                            <i class="fas fa-book me-2"></i>Enrolled Courses
                        </button>
                    </li>
                </ul>

                <!-- Tab Content -->
                <div class="tab-content" id="profileTabsContent">
                    <!-- Overview Tab -->
                    <div class="tab-pane fade show active" id="overview" role="tabpanel">
                        <!-- Quick Stats -->
                        <div class="row g-3 mb-4">
                            <div class="col-md-4">
                                <div class="card border-0 shadow-sm h-100">
                                    <div class="card-body text-center">
                                        <i class="fas fa-file-alt text-primary" style="font-size: 32px;"></i>
                                        <h3 class="fw-bold mt-2 mb-0"><?= $stats['total_exams'] ?? 0 ?></h3>
                                        <span class="text-muted small">Total Exams</span>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="card border-0 shadow-sm h-100">
                                    <div class="card-body text-center">
                                        <i class="fas fa-check-circle text-success" style="font-size: 32px;"></i>
                                        <h3 class="fw-bold mt-2 mb-0"><?= $stats['passed_exams'] ?? 0 ?></h3>
                                        <span class="text-muted small">Passed Exams</span>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="card border-0 shadow-sm h-100">
                                    <div class="card-body text-center">
                                        <i class="fas fa-star text-warning" style="font-size: 32px;"></i>
                                        <h3 class="fw-bold mt-2 mb-0"><?= $stats['best_score'] ?? 0 ?>%</h3>
                                        <span class="text-muted small">Best Score</span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Recent Activity -->
                        <?php if (isset($recent_exams) && !empty($recent_exams)): ?>
                        <div class="card border-0 shadow-sm">
                            <div class="card-header bg-transparent border-0">
                                <h5 class="fw-bold mb-0">Recent Exam Activity</h5>
                            </div>
                            <div class="card-body pt-0">
                                <?php foreach ($recent_exams as $exam): ?>
                                <div class="d-flex justify-content-between align-items-center py-2 border-bottom">
                                    <div>
                                        <h6 class="fw-bold mb-0"><?= htmlspecialchars($exam['title'] ?? 'Exam') ?></h6>
                                        <span class="text-muted small">
                                            <i class="fas fa-calendar-alt me-1"></i>
                                            <?= isset($exam['completed_at']) ? date('M d, Y', strtotime($exam['completed_at'])) : 'Pending' ?>
                                        </span>
                                    </div>
                                    <div>
                                        <span
                                            class="badge <?= ($exam['student_mark'] ?? 0) >= 50 ? 'bg-success' : 'bg-danger' ?> rounded-pill">
                                            <?= $exam['student_mark'] ?? 0 ?>%
                                        </span>
                                    </div>
                                </div>
                                <?php endforeach; ?>
                            </div>
                        </div>
                        <?php else: ?>
                        <div class="card border-0 shadow-sm">
                            <div class="card-body text-center py-4">
                                <i class="fas fa-inbox text-muted" style="font-size: 36px;"></i>
                                <p class="text-muted mt-2">No recent exam activity</p>
                            </div>
                        </div>
                        <?php endif; ?>
                    </div>

                    <!-- Exam History Tab -->
                    <div class="tab-pane fade" id="exams" role="tabpanel">
                        <?php if (isset($exam_history) && !empty($exam_history)): ?>
                        <div class="card border-0 shadow-sm">
                            <div class="card-body p-0">
                                <div class="table-responsive">
                                    <table class="table table-hover mb-0">
                                        <thead>
                                            <tr>
                                                <th>Exam</th>
                                                <th>Course</th>
                                                <th>Date</th>
                                                <th>Score</th>
                                                <th>Status</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <?php foreach ($exam_history as $exam): ?>
                                            <tr>
                                                <td class="fw-bold"><?= htmlspecialchars($exam['title'] ?? 'Exam') ?>
                                                </td>
                                                <td><?= htmlspecialchars($exam['course_name'] ?? 'N/A') ?></td>
                                                <td><?= isset($exam['completed_at']) ? date('M d, Y', strtotime($exam['completed_at'])) : 'N/A' ?>
                                                </td>
                                                <td>
                                                    <span
                                                        class="badge <?= ($exam['student_mark'] ?? 0) >= 50 ? 'bg-success' : 'bg-danger' ?> rounded-pill">
                                                        <?= $exam['student_mark'] ?? 0 ?>%
                                                    </span>
                                                </td>
                                                <td>
                                                    <span
                                                        class="badge <?= ($exam['student_mark'] ?? 0) >= 50 ? 'bg-success' : 'bg-warning' ?>">
                                                        <?= ($exam['student_mark'] ?? 0) >= 50 ? 'Passed' : 'Failed' ?>
                                                    </span>
                                                </td>
                                            </tr>
                                            <?php endforeach; ?>
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                        <?php else: ?>
                        <div class="card border-0 shadow-sm">
                            <div class="card-body text-center py-5">
                                <i class="fas fa-file-alt text-muted" style="font-size: 48px;"></i>
                                <h5 class="fw-bold mt-3">No Exam History</h5>
                                <p class="text-muted small">You haven't taken any exams yet.</p>
                            </div>
                        </div>
                        <?php endif; ?>
                    </div>

                    <!-- Enrolled Courses Tab -->
                    <div class="tab-pane fade" id="courses" role="tabpanel">
                        <?php if (isset($enrolled_courses) && !empty($enrolled_courses)): ?>
                        <div class="row g-3">
                            <?php foreach ($enrolled_courses as $course): ?>
                            <div class="col-md-6">
                                <a href="/api/courses/<?= $course['id'] ?? 0 ?>" class="text-decoration-none">
                                    <div class="card border-0 shadow-sm h-100">
                                        <div class="card-body">
                                            <h6 class="fw-bold mb-2">
                                                <?= htmlspecialchars($course['name'] ?? 'Course') ?></h6>
                                            <span class="badge bg-primary rounded-pill">
                                                <?= $course['exam_count'] ?? 0 ?> Exams
                                            </span>
                                            <span class="badge bg-light text-dark rounded-pill ms-1">
                                                <i class="fas fa-clock me-1"></i>
                                                <?= $course['progress'] ?? 0 ?>% Complete
                                            </span>
                                        </div>
                                    </div>
                                </a>
                            </div>
                            <?php endforeach; ?>
                        </div>
                        <?php else: ?>
                        <div class="card border-0 shadow-sm">
                            <div class="card-body text-center py-5">
                                <i class="fas fa-book-open text-muted" style="font-size: 48px;"></i>
                                <h5 class="fw-bold mt-3">No Courses Enrolled</h5>
                                <p class="text-muted small">You haven't enrolled in any courses yet.</p>
                                <a href="/api/courses" class="btn btn-primary mt-2">Browse Courses</a>
                            </div>
                        </div>
                        <?php endif; ?>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Edit Profile Modal -->
    <div class="modal fade" id="editProfileModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title fw-bold">Edit Profile</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <form method="POST" action="/api/users/<?= $user['id'] ?? 0 ?>" enctype="multipart/form-data">
                    <div class="modal-body">
                        <!-- Image Upload -->
                        <div class="text-center mb-3">
                            <div class="profile-avatar-upload position-relative d-inline-block">
                                <?php if (isset($user['image']) && $user['image']): ?>
                                <img src="<?= htmlspecialchars($user['image']) ?>" alt="Profile" id="profilePreview"
                                    class="rounded-circle"
                                    style="width: 100px; height: 100px; object-fit: cover; border: 3px solid var(--bs-primary);">
                                <?php else: ?>
                                <div class="avatar-placeholder rounded-circle d-flex align-items-center justify-content-center mx-auto"
                                    id="profilePreview"
                                    style="width: 100px; height: 100px; background: linear-gradient(135deg, var(--bs-primary), var(--gold)); color: white; font-size: 36px;">
                                    <?= strtoupper(substr($user['first_name'] ?? 'U', 0, 1)) ?>
                                </div>
                                <?php endif; ?>
                                <label for="imageUpload"
                                    class="position-absolute bottom-0 end-0 bg-primary text-white rounded-circle p-2"
                                    style="cursor: pointer; width: 32px; height: 32px; display: flex; align-items: center; justify-content: center; border: 2px solid white;">
                                    <i class="fas fa-camera" style="font-size: 12px;"></i>
                                </label>
                                <input type="file" id="imageUpload" name="image" accept="image/*" class="d-none"
                                    onchange="previewImage(this)">
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-6 mb-3">
                                <label class="form-label fw-semibold small">First Name</label>
                                <input type="text" class="form-control" name="first_name"
                                    value="<?= htmlspecialchars($user['first_name'] ?? '') ?>" required>
                            </div>
                            <div class="col-6 mb-3">
                                <label class="form-label fw-semibold small">Last Name</label>
                                <input type="text" class="form-control" name="last_name"
                                    value="<?= htmlspecialchars($user['last_name'] ?? '') ?>" required>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-semibold small">Email Address</label>
                            <input type="email" class="form-control" name="email"
                                value="<?= htmlspecialchars($user['email'] ?? '') ?>" required>
                        </div>

                        <?php if (($user['role'] ?? '') === 'teacher'): ?>
                        <div class="mb-3">
                            <label class="form-label fw-semibold small">Teacher Courses</label>
                            <?php if (isset($teacher_courses) && !empty($teacher_courses)): ?>
                            <?php foreach ($teacher_courses as $course): ?>
                            <span class="badge bg-secondary me-1"><?= htmlspecialchars($course['name'] ?? '') ?></span>
                            <?php endforeach; ?>
                            <?php else: ?>
                            <p class="text-muted small">No courses assigned</p>
                            <?php endif; ?>
                        </div>
                        <?php endif; ?>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                        <button type="submit" class="btn btn-primary">Save Changes</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <style>
    /* Profile Container */
    .profile-container {
        max-width: 1200px;
        margin: 0 auto;
    }

    /* Profile Avatar */
    .profile-avatar {
        position: relative;
    }

    .avatar-placeholder {
        font-weight: 700;
    }

    /* Stats */
    .stat-item {
        padding: 8px;
    }

    .stat-item h5 {
        color: var(--bs-primary);
    }

    /* Card hover effects */
    .profile-container .card {
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .profile-container .card:hover {
        transform: translateY(-4px);
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.08) !important;
    }

    /* Tab styling */
    .nav-tabs .nav-link {
        color: var(--ink);
        font-weight: 500;
    }

    .nav-tabs .nav-link.active {
        color: var(--bs-primary);
        border-bottom: 3px solid var(--bs-primary);
    }

    /* Table styling */
    .table thead th {
        background: var(--parchment);
        color: var(--ink);
        font-weight: 600;
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 0.03em;
        border-bottom: 2px solid var(--line);
    }

    /* Modal styling */
    .modal-content {
        border: none;
        border-radius: 12px;
    }

    .modal-header {
        border-bottom: 1px solid var(--line);
    }

    .modal-footer {
        border-top: 1px solid var(--line);
    }
    </style>

    <script>
    // Preview image before upload
    function previewImage(input) {
        if (input.files && input.files[0]) {
            const reader = new FileReader();
            reader.onload = function(e) {
                const preview = document.getElementById('profilePreview');
                if (preview.tagName === 'IMG') {
                    preview.src = e.target.result;
                } else {
                    // If it's the placeholder div, replace with img
                    const img = document.createElement('img');
                    img.id = 'profilePreview';
                    img.src = e.target.result;
                    img.className = 'rounded-circle';
                    img.style.cssText =
                        'width: 100px; height: 100px; object-fit: cover; border: 3px solid var(--bs-primary);';
                    preview.parentNode.replaceChild(img, preview);
                }
            };
            reader.readAsDataURL(input.files[0]);
        }
    }
    </script>
    <?php renderFooter(); ?>
</body>

</html>