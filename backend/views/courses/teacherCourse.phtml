<?php require_once __DIR__ . '/../layout.phtml';
$course = $course ?? null;
$courseExams = $courseExams ?? [];
$courseQuestions = $courseQuestions ?? [];
$courseStudents = $courseStudents ?? []; // Calculate statistics
$totalExams = count($courseExams);
$totalQuestions = count($courseQuestions);
$totalStudents = count($courseStudents);
$activeExams = 0;
$completedExams = 0;
$now = new DateTime();
foreach ($courseExams as $exam) {
    $endDate = new
        DateTime($exam['end_date']);
    if ($now > $endDate) {
        $completedExams++;
    } else {
        $activeExams++;
    }
}
?>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
    <link rel="stylesheet" href="<?= BASE_PATH ?>/utils/css/teacherCourse.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
    <title>Teacher Courses</title>

    <style>

    </style>
</head>

<body>
    <?php renderHeader("Teacher Courses") ?>

    <div class="container w-75 p-5">
        <?php if ($course): ?>
            <!-- Course Header -->
            <div class="card border-0 shadow-sm overflow-hidden mb-4">
                <div class="card-body p-4">
                    <div class="d-flex flex-wrap justify-content-between align-items-start gap-3">
                        <div>
                            <h2 class="fw-bold mb-1"><?= htmlspecialchars($course['name']) ?></h2>
                            <p class="text-muted small mb-0">
                                <i class="fas fa-code me-1"></i>
                                <?= htmlspecialchars($course['course_code'] ?? 'Course Code') ?>
                            </p>
                            <?php if (!empty($course['course_description'])): ?>
                                <p class="mt-2 mb-0 text-muted"><?= htmlspecialchars($course['course_description']) ?></p>
                            <?php endif ?>
                        </div>
                        <div>
                            <span class="badge bg-primary rounded-pill px-4 py-2 fs-6">
                                <i class="fas fa-graduation-cap me-1"></i> Active
                            </span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Statistics Cards -->
            <div class="row g-3 mb-4">
                <div class="col-md-3 col-6">
                    <div class="card border-0 shadow-sm stat-card h-100">
                        <div class="card-body text-center p-3">
                            <i class="fas fa-file-alt text-primary" style="font-size: 1.5rem;"></i>
                            <h3 class="fw-bold mb-0 mt-1"><?= $totalExams ?></h3>
                            <small class="text-muted">Total Exams</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-3 col-6">
                    <div class="card border-0 shadow-sm stat-card h-100">
                        <div class="card-body text-center p-3">
                            <i class="fas fa-question-circle text-success" style="font-size: 1.5rem;"></i>
                            <h3 class="fw-bold mb-0 mt-1"><?= $totalQuestions ?></h3>
                            <small class="text-muted">Total Questions</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-3 col-6">
                    <div class="card border-0 shadow-sm stat-card h-100">
                        <div class="card-body text-center p-3">
                            <i class="fas fa-users text-info" style="font-size: 1.5rem;"></i>
                            <h3 class="fw-bold mb-0 mt-1"><?= $totalStudents ?></h3>
                            <small class="text-muted">Enrolled Students</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-3 col-6">
                    <div class="card border-0 shadow-sm stat-card h-100">
                        <div class="card-body text-center p-3">
                            <i class="fas fa-clock text-warning" style="font-size: 1.5rem;"></i>
                            <h3 class="fw-bold mb-0 mt-1"><?= $activeExams ?></h3>
                            <small class="text-muted">Active Exams</small>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Tabs -->
            <div class="card border-0 shadow-sm overflow-hidden">
                <div class="card-body p-0">
                    <ul class="nav nav-tabs border-0 px-4 pt-3" id="courseTabs" role="tablist">
                        <li class="nav-item" role="presentation">
                            <button class="nav-link active" id="exams-tab" data-bs-toggle="tab" data-bs-target="#exams"
                                type="button" role="tab">
                                <i class="fas fa-file-alt me-2"></i>Exams
                                <span class="badge bg-secondary rounded-pill ms-1"><?= $totalExams ?></span>
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" id="questions-tab" data-bs-toggle="tab" data-bs-target="#questions"
                                type="button" role="tab">
                                <i class="fas fa-question-circle me-2"></i>Questions
                                <span class="badge bg-secondary rounded-pill ms-1"><?= $totalQuestions ?></span>
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" id="students-tab" data-bs-toggle="tab" data-bs-target="#students"
                                type="button" role="tab">
                                <i class="fas fa-users me-2"></i>Students
                                <span class="badge bg-secondary rounded-pill ms-1"><?= $totalStudents ?></span>
                            </button>
                        </li>
                    </ul>

                    <div class="tab-content p-4" id="courseTabsContent">
                        <!-- EXAMS TAB -->
                        <div class="tab-pane fade show active" id="exams" role="tabpanel">
                            <div class="d-flex justify-content-between align-items-center mb-3">
                                <h6 class="fw-bold mb-0"><i class="fas fa-file-alt me-2"></i>Course Exams</h6>
                                <button id="createExamBtn" class="btn btn-primary btn-sm">
                                    <i class="fas fa-plus me-1"></i> Add Exam
                                </button>
                            </div>

                            <?php if (!empty($courseExams)): ?>
                                <div class="exam-grid">
                                    <?php foreach ($courseExams as $exam):
                                        $examEnd = new DateTime($exam['end_date']);
                                        $isCompleted = $now > $examEnd;
                                        $startDate = new DateTime($exam['start_date']);
                                        $daysUntil = $now->diff($startDate)->days;
                                        $isUpcoming = $now < $startDate;
                                    ?>
                                        <div class="card">
                                            <a href="<?= BASE_PATH ?>/api/exams/preview/<?= $exam['id'] ?>?course_id=<?= $course['id'] ?>"
                                                class="exam-card-link card-body text-decoration-none">
                                                <div class="exam-card card border-0 shadow-sm h-100">
                                                    <div class="card-body p-4">
                                                        <!-- Exam Header -->
                                                        <div class="d-flex justify-content-between align-items-start mb-2">
                                                            <h6 class="fw-bold mb-0 text-dark">
                                                                <?= htmlspecialchars($exam['title']) ?>
                                                            </h6>
                                                            <div class="d-flex gap-1">
                                                                <?php if ($isCompleted): ?>
                                                                    <span class="badge bg-success rounded-pill px-3 py-2">
                                                                        <i class="fas fa-check-circle me-1"></i> Completed
                                                                    </span>
                                                                <?php elseif ($isUpcoming): ?>
                                                                    <span class="badge bg-info text-dark rounded-pill px-3 py-2">
                                                                        <i class="fas fa-clock me-1"></i> Upcoming
                                                                    </span>
                                                                <?php else: ?>
                                                                    <span class="badge bg-warning text-dark rounded-pill px-3 py-2">
                                                                        <i class="fas fa-play me-1"></i> Active
                                                                    </span>
                                                                <?php endif ?>
                                                            </div>
                                                        </div>

                                                        <!-- Exam Details -->
                                                        <div class="exam-details mt-2">
                                                            <div class="row g-2">
                                                                <div class="col-6">
                                                                    <small class="text-muted d-block">
                                                                        <i class="fas fa-calendar-alt me-1"></i> Start
                                                                    </small>
                                                                    <small class="fw-semibold">
                                                                        <?= date('M d, Y', strtotime($exam['start_date'])) ?>
                                                                    </small>
                                                                </div>
                                                                <div class="col-6">
                                                                    <small class="text-muted d-block">
                                                                        <i class="fas fa-calendar-check me-1"></i> End
                                                                    </small>
                                                                    <small class="fw-semibold">
                                                                        <?= date('M d, Y', strtotime($exam['end_date'])) ?>
                                                                    </small>
                                                                </div>
                                                            </div>

                                                            <?php if ($isUpcoming): ?>
                                                                <div class="mt-2">
                                                                    <small class="text-muted">
                                                                        <i class="fas fa-hourglass-half me-1"></i>
                                                                        Starts in <?= $daysUntil ?> day<?= $daysUntil > 1 ? 's' : '' ?>
                                                                    </small>
                                                                </div>
                                                            <?php else: ?>
                                                                <div class="my-2">
                                                                    <small class="text-muted">
                                                                        Taken & Completed
                                                                    </small>
                                                                </div>
                                                            <?php endif; ?>
                                                        </div>
                                                    </div>
                                                </div>
                                            </a>
                                            <div
                                                class="exam-footer mt-3 pt-2 border-top d-flex justify-content-between align-items-center card-footer">
                                                <small class="text-muted">
                                                    <i class="fas fa-star me-1"></i>
                                                    <?= $exam['total_marks'] ?? 0 ?> marks
                                                </small>

                                                <form
                                                    action="<?= BASE_PATH ?>/api/exams/delete/<?= $exam['id'] ?>?course_id=<?= $course['id'] ?>"
                                                    method="POST" class="m-0">
                                                    <button type="submit" class="btn btn-sm btn-danger"
                                                        onclick="return confirm('Are you sure you want to delete this exam?')">
                                                        <i class="fas fa-trash-alt"></i>
                                                    </button>
                                                </form>
                                                <a href="<?= BASE_PATH ?>/api/exams/<?= $exam['id'] ?>/details/teacher"
                                                    class="btn btn-sm btn-primary">
                                                    View Details
                                                    <i class="fas fa-arrow-right ms-1"></i>
                                                </a>
                                            </div>
                                        </div>
                                    <?php endforeach ?>
                                </div>
                            <?php else: ?>
                                <div class="empty-state text-center py-5">
                                    <i class="fas fa-file-alt text-muted" style="font-size: 3rem;"></i>
                                    <p class="text-muted mt-3 mb-0">No exams found for this course.</p>
                                    <a href="<?= BASE_PATH ?>/api/courses/<?= $course['id'] ?>/exams/add"
                                        class="btn btn-primary btn-sm mt-2">
                                        <i class="fas fa-plus me-1"></i> Create First Exam
                                    </a>
                                </div>
                            <?php endif ?>
                        </div>

                        <!-- QUESTIONS TAB -->
                        <div class="tab-pane fade" id="questions" role="tabpanel">
                            <div class="d-flex justify-content-between align-items-center mb-3">
                                <h6 class="fw-bold mb-0"><i class="fas fa-question-circle me-2"></i>Course Questions
                                </h6>
                                <a href="<?= BASE_PATH ?>/api/questions/create/courses/<?= $course['id'] ?>/view"
                                    class="btn btn-primary btn-sm">
                                    <i class="fas fa-plus me-1"></i> Add Question
                                </a>
                            </div>

                            <?php if (!empty($courseQuestions)): ?>
                                <div class="list-group">
                                    <?php foreach ($courseQuestions as $question): ?>
                                        <a href="<?= BASE_PATH ?>/api/questions/<?= $question['id'] ?>/edit"
                                            class="list-group-item list-group-item-action clickable-item d-flex justify-content-between align-items-center">
                                            <div class="flex-grow-1 me-3">
                                                <h6 class="mb-1 fw-semibold"><?= htmlspecialchars($question['question']) ?>
                                                </h6>
                                                <small class="text-muted">
                                                    <i class="fas fa-tag me-1"></i>
                                                    <?= htmlspecialchars($question['question_type'] ?? 'Multiple Choice') ?>
                                                    <span class="mx-2">|</span>
                                                    <i class="fas fa-star me-1"></i>
                                                    <?= $question['question_mark'] ?? 2 ?> marks
                                                </small>
                                            </div>
                                            <div>
                                                <?php if (!empty($question['exam_id'])): ?>
                                                    <span class="badge bg-info rounded-pill me-2">In Exam</span>
                                                <?php else: ?>
                                                    <span class="badge bg-secondary rounded-pill me-2">Unused</span>
                                                <?php endif ?>
                                                <i class="fas fa-chevron-right text-muted small"></i>
                                            </div>
                                        </a>
                                    <?php endforeach ?>
                                </div>
                            <?php else: ?>
                                <div class="empty-state text-center">
                                    <i class="fas fa-question-circle text-muted" style="font-size: 3rem;"></i>
                                    <p class="text-muted mt-3 mb-0">No questions found for this course.</p>
                                    <a href="<?= BASE_PATH ?>/api/courses/<?= $course['id'] ?>/questions/add"
                                        class="btn btn-primary btn-sm mt-2">
                                        <i class="fas fa-plus me-1"></i> Create First Question
                                    </a>
                                </div>
                            <?php endif ?>
                        </div>

                        <!-- STUDENTS TAB -->
                        <div class="tab-pane fade" id="students" role="tabpanel">
                            <div class="d-flex justify-content-between align-items-center mb-3">
                                <h6 class="fw-bold mb-0"><i class="fas fa-users me-2"></i>Enrolled Students</h6>
                                <a href="<?= BASE_PATH ?>/api/courses/<?= $course['id'] ?>/students/add"
                                    class="btn btn-primary btn-sm">
                                    <i class="fas fa-user-plus me-1"></i> Add Student
                                </a>
                            </div>

                            <?php if (!empty($courseStudents)): ?>
                                <div class="table-responsive">
                                    <table class="table table-hover align-middle">
                                        <thead class="table-light">
                                            <tr>
                                                <th>#</th>
                                                <th><i class="fas fa-user me-1"></i> Name</th>
                                                <th><i class="fas fa-envelope me-1"></i> Email</th>
                                                <th><i class="fas fa-calendar me-1"></i> Enrolled</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <?php foreach ($courseStudents as $index => $student): ?>
                                                <tr>
                                                    <td><?= $index + 1 ?></td>
                                                    <td>
                                                        <a href="<?= BASE_PATH ?>/api/students/<?= $student['student_id'] ?>/edit"
                                                            class="text-decoration-none fw-semibold text-dark">
                                                            <?= htmlspecialchars($student['first_name'] . " " . $student['last_name']) ?>
                                                        </a>
                                                    </td>
                                                    <td><?= htmlspecialchars($student['email'] ?? 'N/A') ?></td>
                                                    <td><?= date('M d, Y', strtotime($student['enrolment_date'] ?? 'now')) ?>
                                                    </td>
                                                </tr>
                                            <?php endforeach ?>
                                        </tbody>
                                    </table>
                                </div>
                            <?php else: ?>
                                <div class="empty-state text-center">
                                    <i class="fas fa-users text-muted" style="font-size: 3rem;"></i>
                                    <p class="text-muted mt-3 mb-0">No students enrolled in this course.</p>
                                    <a href="<?= BASE_PATH ?>/api/courses/<?= $course['id'] ?>/students/add"
                                        class="btn btn-primary btn-sm mt-2">
                                        <i class="fas fa-user-plus me-1"></i> Enroll First Student
                                    </a>
                                </div>
                            <?php endif ?>
                        </div>
                    </div>
                </div>
            </div>
        <?php else: ?>
            <!-- Course Not Found -->
            <div class="card border-0 shadow-sm">
                <div class="card-body text-center py-5">
                    <i class="fas fa-exclamation-triangle text-warning" style="font-size: 3rem;"></i>
                    <h4 class="mt-3">Course Not Found</h4>
                    <p class="text-muted">The requested course could not be found.</p>
                    <a href="<?= BASE_PATH ?>/teacher/dashboard" class="btn btn-primary">
                        <i class="fas fa-arrow-left me-1"></i> Back to Dashboard
                    </a>
                </div>
            </div>
        <?php endif ?>
    </div>
    <script>
        const basePath = "<?= BASE_PATH ?>";
        const courseID = '<?= $course['id'] ?? '' ?>';
    </script>
    <script src="<?= BASE_PATH ?>/utils/js/modal.js"></script>
    <script src="<?= BASE_PATH ?>/utils/js/createExam.js"></script>

    <?php renderFooter() ?>

</body>

</html>